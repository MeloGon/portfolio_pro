enum LanguageNames {
  flutter('flutter'),
  android('android'),
  kotlin('kotlin'),
  html('html'),
  swift('swift'),
  unknown('unknown');

  final String language;

  const LanguageNames(this.language);
}
